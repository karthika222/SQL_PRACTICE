select s.student_name, e.course_id, c.course_name, e.grade from students s 
join enrollments e on s.student_id=e.student_id 
join courses c c.course_id= e.course_id where
e.grade='A';
/* As course name is not directly in the enrollments we are using join again with enrollments*/
